@model IEnumerable<Event_Management_System_BootCamp.DbContext.Services>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Decoration.Flowers)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dj.Speaker)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Menu.MenuName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Decoration.Flowers)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dj.Speaker)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Menu.MenuName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
